spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blogs
    username: root
    password:
  rabbitmq:
    host: localhost
    port: 5672
    username:
    password: 1
    virtualHost:
    listener:
      simple:
        prefetch: 1
        retry:
          enabled: true

file:
  upload:
    savePath: /xxx/image
    relativePath: /image
    saveArticlePath: /xxx/article
    relativeArticlePath: /article
  jwtKeyPath: /xxx/jwtKey.json